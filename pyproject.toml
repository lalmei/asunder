[tool.poetry]
name = "asunder"
version = "0.1.0"
description = ""
authors = ["Leandro G. Almeida <leandro.almeida@climate.com>"]
readme = "README.md"
packages = [{include = "asunder", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
